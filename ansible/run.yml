- name: Run single role
  hosts: all
  become: true

  vars:
    ansible_user: '{{ user }}'
    ansible_port: '{{ ssh_port }}'
    ansible_ssh_private_key_file: '{{ user_ssh_private_key_path }}'

  tasks:
    - name: Run role
      ansible.builtin.include_role:
        name: '{{ role }}'
