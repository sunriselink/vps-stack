- name: Update apt cache and upgrade packages
  ansible.builtin.apt:
    update_cache: true
    upgrade: dist

- name: Install required packages
  ansible.builtin.apt:
    name:
      - sudo
      - curl
      - htop
      - git
      - mc
    state: present
